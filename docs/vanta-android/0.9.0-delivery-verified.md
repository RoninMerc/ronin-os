# Ronin Vanta Android 0.9.0 — verified delivery

## Exact candidate

Source commit: bab2579c3a11acab782686a1b2d1f4cdd278f1f1.
Branch: fix/vanta-090-android-pipeline.
Workflow 34237081707: build, Android 29 and Android 36 all completed successfully.
Build artifact 10060498158; reports 10060499064; Android 29 evidence 10060938547; Android 36 evidence 10061033626.
Version 0.9.0 / versionCode 90. Java 17, Gradle 8.13, AGP 8.11.1, compile/target SDK 36, min SDK 29.

## Implemented repairs

- Unified six-attempt Forge budget; the upload helper previously rejected attempts after the third.
- New Forge workflows use checkpointed file-by-file authoring and targeted compiler-driven repairs, not one whole-project response. Explicit output-token cutoffs can continue bounded saved source; ambiguous dropped paid responses are not automatically repurchased.
- Validate private worker access before paid source generation. Distinguish build connection/permission errors from AI model failures.
- Build retrieval uses saved commit/run identities and scheduler checks. Retrieving a finished APK does not require a working coding API key. Local signing/publication checkpoints survive retry without another build.
- Sign and verify the actual generated APK on the phone before publishing to Downloads/Ronin Vanta. Preserve device-bound generated-app signing identity.
- Shared model-specific image form/request validation, response-type checking, base64/data-URI/URL handling and saved delivery URL recovery. Invalid/empty outputs are rejected; near-uniform dark/white/transparent images require inspection and are not labelled successful generations.
- Featherless image-prompt helper workflows require explicit native renderer selection and cross-provider approval. No native image-rendering endpoint is fabricated for text/vision inference.
- Supported text/image/reference-to-video inputs are validated before queueing. Status JSON is distinguished from binary video even under incorrect MIME. Preserve newly returned completion/download URL before retrieval and recover without buying another generation.
- Optional Featherless facet failures do not erase the retrieved catalogue. Unverified adapter aliases and provider-removed entries are not falsely marked available.
- Redacted encrypted diagnostics expose operation, endpoint, HTTP status and actual compiler log with recovery actions.
- Existing chat, Prompt Architect, provider integrations, voice, virtualized model browser, history, Library, encrypted storage, jobs/progress/background architecture retained. No Windows desktop or unrelated Ronin source changed.

## Completed verification

JVM unit/provider-contract tests: 260 passed, zero failures/errors/skips.
Android 10/API 29: 79 debug + 77 release-payload tests passed.
Android 16/API 36: 79 debug + 77 release-payload tests passed.
Separate upgrade prepare/verify and process prepare/verify probes passed on each version.
Final captured Vanta debug/release logs contained no FATAL EXCEPTION, ANR or WindowLeaked entries.
Lint: zero errors, 105 warnings (94 SetTextI18n plus existing dependency/design advisories). Gradle deprecation warnings remain documented.
Actual generated verification APK was collected, signed, saved, installed and launched on both emulators. Its activity displayed VANTA BUILD VERIFIED.
Both delivery APK archives, manifest, DEX, non-debuggable release flag, version/package and v1/v2/v3 signatures were verified locally.
Both signing certificates match their respective previous 0.8.1 tracks. Non-signature compiled entries match the CI release candidates byte-for-byte. Test fixture assets are absent from production APKs.

## Delivered files

Ronin-Vanta-0.9.0.apk: 6,838,469 bytes; package com.ronin.vanta.fixed.
SHA-256: 983a14ce75caef33097c276bfb7f63c53d4924662003067355a3ac9468fdda94.
Certificate SHA-256: e67ead57ec87b387a898b6e804e9c5207b254e9fd1393eab5c2ee60a5115ce47.

Ronin-Vanta-0.9.0-Personal-Update.apk: 6,838,469 bytes; package com.ronin.vanta.personal.
SHA-256: a09f3947e2a0a14ddab3e396a13b9c98eb37afec5eb6fda6d7407eae995f4905.
Certificate SHA-256: 113a98302e5642c6f5e2a3df1da892a848115a80f7911a37c25f9a49e21c6c94.

No signing private keys, tokens or user credentials were uploaded with this record.

## Evidence boundaries

The existing private Android worker actually compiled an intentionally broken benign fixture, returned real compiler errors, then compiled the repaired fixture and ran its unit tasks/lint. Runs: 34228535836 and 34229574606. No worker implementation was modified.

Android pipeline tests exercised real Vanta orchestration/collection/signing/publication with the resulting compiled APK and controlled HTTP/AI responses. No live paid-user AI credentials were available. Live generated application quality and arbitrary provider prompts were not verified.

Background tests wrote 200 encrypted records through the actual engine while Home/another app/screen-off transitions occurred for more than three minutes; then restored the Activity and result. Connectivity tests used controlled execution responses and verified one accepted submission and saved remote identity. Process-death and update probes checked encrypted credentials/conversations/drafts, remote IDs, ambiguous-request handling and signing identity.

The release payload was installed with a disposable CI test certificate. Delivery packages were subsequently owner-signed and verified locally; the owner-signed downloads were not installed on the user's physical phone. No universal OEM/battery-policy, physical audio/camera or 100% reliability guarantee is claimed.

Android compilation still requires the phone's private build-service connection, separate from an AI API key. Windows EXE compilation from this phone app requires a Windows compiler endpoint; source is not misrepresented as an executable. Provider content restrictions and account entitlements remain provider-controlled; no bypass or promise of universally unrestricted generation is made. Provider-labelled uncensored models remain discoverable subject to actual capabilities.
