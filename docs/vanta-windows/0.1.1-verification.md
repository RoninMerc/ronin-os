# Ronin Vanta Windows 0.1.1 — release verification

## Exact build

Source commit: `df60e82b5dff273a9a603d4b3c1db70a41a1db83`.
Workflow: `34220834221`, completed successfully.
Artifact: `10053834688`, `vanta-windows-build`.
Downloaded artifact SHA-256: `2b43e3277fe89018b0f1c78281da0f655acedee43f0296c21656091b3cd8ff39`.

Installer: `Ronin-Vanta-Windows-0.1.1-Setup.exe`.
Size: 53,811,784 bytes.
Installer SHA-256: `5f538681e0f3b6a6c51b8262756eed9396ee0418449004a277bf9570283d68e9`.
Downloaded hash matched the runner's result. Installer MZ/PE structure and source ZIP integrity checked after retrieval.
Authenticode status: **NotSigned**. No commercial publisher-signing certificate was available. These are integrity checks, not trusted-publisher or antivirus certification.

## Application and scope

Independent native WPF/.NET 10 Windows client, version 0.1.1, self-contained win-x64 installer targeting Windows 11 build 22000+.
All AI inference is API-based: OpenAI, Anthropic, Venice AI and Featherless AI.
Forge and Prompt Architect default to `huihui-ai/Huihui-Qwen3.5-27B-abliterated` when configured and available; absent default does not silently fall back. Auto / Best and manual model choice remain deliberate alternatives.
Android and unrelated Ronin products were not modified.

## Executed checks

Application compilation: 0 errors, 0 warnings.
Native test-harness compilation: 0 errors, 0 warnings.
Automated protocol/workflow/native-desktop checks: **141 passed, 0 failed**.
Self-contained publication: passed.
Installer compilation: passed.
Actual installer installed the application, launched the installed executable, minimized/restored it and closed it with exit code 0.
Reinstallation and second launch passed. The existing database and account encryption identity were retained.
Uninstallation passed; the separate private profile/database and encryption identity remained intact.
Final fresh/reinstall and native workspace screenshots were retrieved and inspected.

## Measured observations

Host: Windows Server 2025, Microsoft Windows NT 10.0.26100, x64 — not the user's physical PC.
Installed fresh startup to usable main-window handle: 682.9408 ms.
Installed post-reinstall startup: 681.8127 ms.
Fresh working set: 114.62109375 MiB; post-reinstall: 111.80859375 MiB.
Sampled idle CPU time over 12 seconds: 0 ms on both observations; timer granularity does not establish literally zero CPU use.
50,000-model filter: 55.9531 ms. Native grid realised 7 rows, not 50,000.
1,500-message layout: 137.787 ms. Native transcript realised 3 rows.
Background workload observation: 120 seconds while minimized and another app had focus.
Five installed Windows voices discovered; an actual WAV was synthesized using an installed voice.
These are individual test-host observations, not general performance guarantees.

## Real Forge verification

A deliberately broken C# project produced actual .NET compiler errors, was repaired/rebuilt/published, and its resulting executable was run and produced `VANTA_REAL_BUILD_OK`.
The Forge orchestration test also read real compiler errors and rebuilt using controlled HTTP repair/review responses. Compiler processes/files/artifacts were real; AI-provider replies were fixtures.
Windows distributions retain the EXE and required runtime/support files in a complete ZIP.
Edited-source rebuild creates a new recorded project version; previous source/artifacts are not overwritten.
The current local compiler adapter supports .NET projects. Other stacks require their own adapters.
Local build/test execution needs explicit user approval and runs with that Windows account's permissions; it is not sandboxed.
Android compilation consumes the existing configured worker. It was not run live in this Windows pass; no worker was modified. Generated-app runtime/unit tests and stable signing are not claimed.

## Remaining verification and feature boundaries

Provider integration tests used isolated HTTP fixtures, not live paid user accounts. Live model entitlement, output quality, cloud audio and model-driven repair remain account-dependent and unverified here.
The user's physical Windows PC, microphone/Bluetooth path, actual DPI transitions and multi-monitor hotplug were not tested. Native window bounds, layout scaling from 100% to 200%, ultrawide layout and minimize/restore were exercised.
Background execution was tested while the process remained running. Exit/shutdown/sleep continuation is not claimed. Interrupted paid synchronous requests are not automatically purchased again.
No commercial subscription backend, cross-device cloud sync, arbitrary local LLM/voice runtime or full-duplex calling was added.
No independent security audit or blanket 100% reliability claim is made.
